# pregnancy 0.2.0

* Add a `medications_simple` data frame
* Add an (unexported) `update_meds_table()` function, to help keep README and vignette current
* pregnancy vignette now sets all dates relative to `Sys.Date()`, to avoid CRAN failures as time passes
* README now sets all dates relative to `Sys.Date()`


# pregnancy 0.1.1

* Update `Description` in response to CRAN submission comments

# pregnancy 0.1.0

* Initial CRAN submission.
